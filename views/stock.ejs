<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stock</title>
</head>
<body>
    <% product.forEach(product => { %>
        <form method="post" action="/product/stock" style="margin-bottom: 20px;">
            <label>Enter the stock of the <%= product.productName %></label>
            <input type="number" name="<%= product.productName %>" value="PATCH" placeholder="Like30,40,50....." />
            <button type="submit">Submit</button>
        </form>
    <% }) %>
</body>
</html>